(function(angular) {
    'use strict'

    angular
        .module('expresso.modules.ong', [])
        .config(config);

        function config ($stateProvider, $urlRouterProvider) {

        }

})(angular);
